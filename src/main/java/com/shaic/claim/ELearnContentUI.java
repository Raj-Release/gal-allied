// package com.shaic.claim;

// import com.vaadin.server.VaadinRequest;
// import com.vaadin.ui.Panel;
// import com.vaadin.ui.UI;
// import com.vaadin.v7.ui.VerticalLayout;

// public class ELearnContentUI extends UI{

	// @Override
	// protected void init(VaadinRequest request) {
		
		// getPage().setTitle("E Learn Document");
		// if(request.getWrappedSession().getAttribute("contentLayout") != null) {
			
			// VerticalLayout vLayout = (VerticalLayout) request.getWrappedSession().getAttribute("contentLayout");
				// Panel panel = new Panel();
				// panel.setContent(vLayout);
				// panel.setHeight("100%");
				// setContent(panel);
		// }		
   // }	
// }